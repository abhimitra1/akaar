{
  "name": "akaar-instantmesh-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Public-facing proxy for the InstantMesh reconstruction API. Runs on the GPU workstation, fixes InstantMesh's CORS bug, requires a valid Supabase login, and rate-limits job submissions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-proxy-middleware": "^2.0.6",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  }
}
